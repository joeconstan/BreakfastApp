<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/settingslayout"
    android:background="@drawable/backgroundrevised">

    <android.support.v7.widget.Toolbar
        android:id="@+id/custom_toolbar_settings"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:orientation="vertical"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        >


        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/toolbartext"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="left"
                android:text="Settings"
                android:layout_marginTop="4dp"
                android:textColor="#ffffff"
                android:textSize="22sp" />

            <ImageButton
                android:id="@+id/toolbar_friend_request_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_marginRight="20sp"
                android:background="@null"
                android:paddingRight="5sp"
                android:layout_marginTop="4dp"
                android:src="@drawable/ic_friend_requests"
                />

        </RelativeLayout>

    </android.support.v7.widget.Toolbar>


    <EditText
        android:id="@+id/settingsName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="name"
        android:layout_below="@+id/custom_toolbar_settings"
        />

    <ImageButton
        android:id="@+id/settingsImageButton"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_below="@+id/settingsName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp" />

    <TextView
        android:id="@+id/settingsChangeBackgroundtv"
        android:layout_width="wrap_content"
        android:text="Change Background"
        android:textSize="18sp"
        android:layout_marginLeft="10dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/settingsImageButton"
        android:layout_marginTop="20dp" />

    <TextView
        android:id="@+id/settingsChangePasswordtv"
        android:layout_width="wrap_content"
        android:text="Change Password"
        android:textSize="18sp"
        android:layout_marginLeft="10dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/settingsChangeBackgroundtv"
        android:layout_marginTop="15sp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="10dp"
        android:id="@+id/settingsSaveButton"
        />


</RelativeLayout>